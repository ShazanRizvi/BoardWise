{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:dev": "./start-dev.sh",
    "build": "lerna run build",
    "dev": "npm run start:dev",
    "migrate:dev": "npx prisma migrate dev --schema=./packages/server/prisma/schema.prisma"

  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^9.0.0"
  }
}
